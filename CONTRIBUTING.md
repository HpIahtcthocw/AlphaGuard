# Contributing

感谢你帮助 PIO 变得更可信。金融系统的贡献优先级是正确性、可复现性和可停止性，而不是功能数量。

## 开始

```powershell
python -m pip install -e ".[dev]"
python -m pytest -q
```

提交应包含：问题定义、数据/接口来源、许可证说明、失败模式、测试和对现有闭环的影响。行情适配器必须标记实时性与延迟；策略必须提供基线和样本外验证；经纪商适配器必须保持人工批准、幂等和杀开关。

适合首次贡献的方向包括券商 CSV 字段映射、数据质量规则、测试样例、文档和无外部副作用的研究指标。
